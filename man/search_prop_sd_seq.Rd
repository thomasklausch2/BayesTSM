% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search_prop_sd_seq.R
\name{search_prop_sd_seq}
\alias{search_prop_sd_seq}
\title{Heuristic search for a Metropolis-Hastings proposal standard deviation (sequential processing)}
\usage{
search_prop_sd_seq(
  m,
  mc = 3000,
  succ.min = 3,
  acc_bounds = c(0.21, 0.24),
  silent = F
)
}
\arguments{
\item{m}{A fitted model object containing acceptance information and current
proposal settings. This object is updated iteratively during the search.}

\item{mc}{Integer; number of additional MCMC iterations used at each tuning
step. Defaults to \code{3000}.}

\item{succ.min}{Integer; number of successive successful tuning iterations
required before the search stops. Defaults to \code{3}.}

\item{acc_bounds}{Numeric vector of length 2 giving the lower and upper
target bounds for the acceptance rate of the \code{X} parameter update. Defaults
to \code{c(0.2, 0.3)}.}

\item{silent}{When output should be produced (FALSE) or not (TRUE).}
}
\value{
A list with the following components:
\describe{
\item{\code{prop.sd.X}}{The tuned proposal standard deviation for the \code{X}
update.}
\item{\code{ac.X}}{The final acceptance rate used for the stopping decision.}
}
}
\description{
Tunes the Metropolis-Hastings proposal standard deviation by iteratively
extending a \link{bayestsm} fitted model run and adjusting the proposal scale until the
observed acceptance rate falls within a target interval for a specified
number of successive iterations.
}
\details{
Like \code{\link[=search_prop_sd]{search_prop_sd()}} but instead of parallel processing through \link{bayestsm} sequential processing with a \code{for} loop through \link{bayestsm_seq} is done. This can be helpful in Monte Carlo simulation studies where parallelization within \code{bayestsm} is not desirable.
}
