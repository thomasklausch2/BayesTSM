% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_IC.r
\name{get_IC}
\alias{get_IC}
\title{Compute information criteria for a fitted \code{bayestsm} model}
\usage{
get_IC(mod, samples = nrow(mod$par.X[[1]]) - 1, warmup = NULL, cores = 2)
}
\arguments{
\item{mod}{A fitted model object returned by \code{\link[=bayestsm]{bayestsm()}}.}

\item{samples}{Integer; number of posterior draws used to approximate the
information criteria. Defaults to the number of post-burn-in draws in the
first chain of \code{mod$par.X} after removing the first \code{warmup} iterations per chain.
Has to be smaller than \code{nrow(mod$par.X[[1]])} minus \code{warmup}.}

\item{warmup}{The number of iterations discarded for warmup per chain.
The default is \code{NULL} in which case, conservatively,
the first half of posterior samples is discarded.}

\item{cores}{Optional integer; number of cores used for parallel evaluation
of the observation-level likelihood contributions. Defaults to 2. To set to maximum available cores set to \code{NULL}.}
}
\value{
A numeric matrix with one row and three columns:
\describe{
\item{\code{WAIC1}}{The first WAIC estimate based on the mean log pointwise
posterior density and a bias correction using the average log likelihood.}
\item{\code{WAIC2}}{The second WAIC estimate based on the mean log pointwise
posterior density and a bias correction using the posterior variance of the
log likelihood contributions.}
\item{\code{DIC}}{The deviance information criterion.}
}
}
\description{
Computes DIC, WAIC1, and WAIC2 for a fitted \code{bayestsm} model from posterior
draws of the model parameters.
}
\details{
The function by removes the first \code{warmup} iteration from the MCMC
output stored in \code{mod$par.X} and \code{mod$par.S}, then samples \code{samples}
posterior draws from the remaining iterations. If \code{warmup} is not specified, the first half of draws is removed, which is a conservative choice.
For each sampled draw, the observation-level likelihood contributions are computed, and
these are combined into the reported information criteria.

Lower values of DIC, WAIC1, and WAIC2 indicate better expected predictive fit,
but these criteria should primarily be compared between models fitted to the
same data.
}
\examples{
\dontrun{
# For a bayestsm model output saved in mod_slice (see ?bayestsm examples), run:
get_IC(mod_slice, warmup = 500)
}

}
\seealso{
\code{\link[=bayestsm]{bayestsm()}}
}
