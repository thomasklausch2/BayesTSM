% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bayestsm_seq.R
\name{bayestsm_seq}
\alias{bayestsm_seq}
\title{Bayesian Accelerated Failure-Time Three-State Model with Censoring After Intervention (sequential processing)}
\usage{
bayestsm_seq(
  d = NULL,
  L = NULL,
  R = NULL,
  Z.X = NULL,
  Z.S = Z.X,
  dist.X = "weibull",
  dist.S = "weibull",
  beta.prior = "t",
  beta.prior.X = 4,
  beta.prior.S = beta.prior.X,
  sig.prior.X = 1,
  sig.prior.S = 1,
  fix.sigma.X = FALSE,
  fix.sigma.S = FALSE,
  mc = 10000,
  chains = 2,
  thinning = 1,
  warmup = mc * 0.05,
  prop.sd = NULL,
  update_till_convergence = FALSE,
  mc_update = mc,
  min_R = 1.01,
  min_eff = chains * 100,
  maxit = NULL,
  prev.run = NULL,
  MH = FALSE,
  rescale_times = TRUE,
  slicesampler_stepsize = 1,
  silent = FALSE,
  log_prior_fun = log_aft_prior,
  do_cpp = TRUE,
  seed_chains = NULL
)
}
\arguments{
\item{d}{Numeric vector indicating the observed outcome category for each
individual. Allowed values are \code{1}, \code{2}, and \code{3}.}

\item{L}{Numeric vector of left interval bounds. Minimum allowed value is \code{0}.}

\item{R}{Numeric vector of right interval bounds. Use \code{Inf} for right-censored
observations. If \code{R = Inf}, \code{d} must be \code{1}. If \code{R < Inf}, \code{d} must be \code{2}
or \code{3}.}

\item{Z.X}{Optional design matrix of covariates for the first transition model
for latent time \code{X}.}

\item{Z.S}{Optional design matrix of covariates for the second transition model
for latent time \code{S}. Defaults to \code{Z.X}.}

\item{dist.X}{Character string specifying the distribution for the \code{X}
transition model. Allowed values are \code{"weibull"}, \code{"lognormal"}, and
\code{"loglog"}.}

\item{dist.S}{Character string specifying the distribution for the \code{S}
transition model. Allowed values are \code{"weibull"}, \code{"lognormal"}, and
\code{"loglog"}.}

\item{beta.prior}{Character string specifying the default prior family for
regression coefficients when \code{log_prior_fun = log_aft_prior}. Allowed values
are \code{"t"} for a Student-\eqn{t} prior and \code{"norm"} for a normal prior.
Default is \code{"t"}.}

\item{beta.prior.X}{Prior hyperparameter for the regression coefficients in
the \code{X} transition model. If \code{log_prior_fun = log_aft_prior} and
\code{beta.prior = "t"}, this is the degrees of freedom of the Student-\eqn{t}
prior. If \code{beta.prior = "norm"}, this is the standard deviation of the
normal prior. For custom prior functions, this value is passed as \code{tau}.
Default is \code{4}.}

\item{beta.prior.S}{Prior hyperparameter for the regression coefficients in
the \code{S} transition model. Interpreted in the same way as \code{beta.prior.X}.
For custom prior functions, this value is passed as \code{tau}. Defaults to
\code{beta.prior.X}.}

\item{sig.prior.X}{Prior hyperparameter for the scale parameter \code{sigma_X} of
the \code{X} transition model. When \code{fix.sigma.X = FALSE}, this value is passed
as \code{sig.prior} to the prior function for the \code{X} model. When
\code{fix.sigma.X = TRUE}, it is interpreted as the fixed value of \code{sigma_X}.
Default is \code{1}.}

\item{sig.prior.S}{Prior hyperparameter for the scale parameter \code{sigma_S} of
the \code{S} transition model. Interpreted in the same way as \code{sig.prior.X}.
When \code{fix.sigma.S = TRUE}, this is the fixed value of \code{sigma_S}. Default is
\code{1}.}

\item{fix.sigma.X}{Logical; whether to fix the scale parameter \code{sigma_X} at
\code{sig.prior.X}.}

\item{fix.sigma.S}{Logical; whether to fix the scale parameter \code{sigma_S} at
\code{sig.prior.S}.}

\item{mc}{Integer; number of Gibbs iterations per chain.}

\item{chains}{Integer; number of MCMC chains.}

\item{thinning}{Integer; thinning interval applied when constructing the
post-sampling output.}

\item{warmup}{Numeric; number of original Gibbs iterations to omit as warmup
before assessing convergence. Internally this is converted to stored
post-thinning iterations using \code{round(warmup / thinning)}. \code{warmup} should be significantly smaller than \code{mc}; default \code{mc * 0.05}.}

\item{prop.sd}{Proposal scale for the random-walk Metropolis-Hastings update.
Used only when \code{MH = TRUE}. This may be a proposal standard deviation or
proposal covariance specification used for the parameter updates. If \code{NULL}
and \code{MH = TRUE}, the function runs a short preliminary sampler and searches
for a useful proposal scale automatically.}

\item{update_till_convergence}{Logical; whether to automatically extend the
MCMC run until the convergence criteria based on \code{min_R} and \code{min_eff} are
met, or until \code{maxit} is reached.}

\item{mc_update}{Integer; number of Gibbs iterations per chain to add when
updating a previous run or when automatic convergence updating is enabled.
Defaults to \code{mc}.}

\item{min_R}{Numeric; target upper threshold for convergence based on the
potential scale reduction factor. Used when \code{update_till_convergence = TRUE}.
Default is \code{1.1}.}

\item{min_eff}{Numeric; target minimum effective sample size. Used when
\code{update_till_convergence = TRUE}. Default is \code{chains * 100}.}

\item{maxit}{Optional integer; maximum number of automatic update cycles when
\code{update_till_convergence = TRUE}. If \code{NULL}, no explicit maximum number of
update cycles is imposed by this argument.}

\item{prev.run}{Optional fitted object returned by a previous call to
\code{bayestsm()}. If supplied, the sampler continues from the last sampled
parameter values and reuses the stored data, priors, proposal settings,
distributions, fixed-scale settings, and other model options from the
previous run.}

\item{MH}{Logical; whether to use random-walk Metropolis-Hastings updates for
the transition-model parameters. If \code{FALSE}, slice-sampling updates are
used. Default is \code{FALSE}.}

\item{rescale_times}{Logical; whether to rescale \code{L} and \code{R} by the median of
the finite values of \code{R} before sampling. Returned data are stored on the
original time scale. Default is \code{TRUE}.}

\item{slicesampler_stepsize}{Numeric step-out size used by the slice sampler
when \code{MH = FALSE}. Default is \code{1}.}

\item{silent}{Logical; whether to suppress progress messages.}

\item{log_prior_fun}{Function used to evaluate the log-prior density for the
AFT transition-model parameters. The default is \code{log_aft_prior}. Custom
prior functions should accept the arguments \code{eta}, \code{tau}, and \code{sig.prior};
see \code{\link[=log_aft_prior]{log_aft_prior()}} for details.}

\item{do_cpp}{Logical; whether to use the C++ implementation of the Gibbs
sampler. If \code{FALSE}, the R implementation is used. The samplers are equivalent, but the C++ implementation is faster.}

\item{seed_chains}{Integer; A vector of length \code{length(chains)} with seeds to which the MCMC chains will be initialized using \code{set.seed}. If \code{NULL}, random seeds are generated.}
}
\value{
A list with the following components:
\describe{
\item{\code{par.X}}{An \code{mcmc.list} containing the stored sampled draws of the
\code{X} transition-model parameters for all chains. The final column is returned
on the natural scale as \code{sigma}.}
\item{\code{par.S}}{An \code{mcmc.list} containing the stored sampled draws of the
\code{S} transition-model parameters for all chains. The final column is returned
on the natural scale as \code{sigma}.}
\item{\code{X}}{Numeric vector containing sampled latent \code{X} values, concatenated
over chains.}
\item{\code{S}}{Numeric vector containing sampled latent \code{S} values, concatenated
over chains.}
\item{\code{ac}}{Acceptance indicators for the Metropolis-Hastings parameter
updates.}
\item{\code{ac.cur}}{Acceptance indicators from the current extension run only,
returned when \code{prev.run} is supplied.}
\item{\code{dat}}{A \code{data.frame} containing the data used in fitting, with
columns \code{d}, \code{L}, and \code{R} on the original time scale.}
\item{\code{priors}}{A list containing the prior hyperparameters
\code{beta.prior.X}, \code{beta.prior.S}, \code{sig.prior.X}, and \code{sig.prior.S}.}
\item{\code{thinning}}{The thinning interval used to construct \code{par.X} and
\code{par.S}. For long MCMC chains, larger \code{thinning} values reduce memory
burden.}
\item{\code{Z.S}}{The covariate matrix used for the \code{S} transition model.}
\item{\code{Z.X}}{The covariate matrix used for the \code{X} transition model.}
\item{\code{prop.sd}}{The proposal tuning specification used for the
Metropolis-Hastings updates.}
\item{\code{dist.X}}{The distribution used for the \code{X} transition model.}
\item{\code{dist.S}}{The distribution used for the \code{S} transition model.}
\item{\code{fix.sigma.X}}{Logical; whether \code{sigma_X} was fixed during fitting.}
\item{\code{fix.sigma.S}}{Logical; whether \code{sigma_S} was fixed during fitting.}
\item{\code{warmup}}{The number of original Gibbs iterations omitted as warmup
before convergence assessment.}
\item{\code{beta.prior}}{The prior family used by the default prior function,
either \code{"t"} or \code{"norm"}.}
\item{\code{seed_chains}}{Integer vector of random seeds used for the chains.}
\item{\code{runtime}}{Total runtime accumulated over the initial run and any
extension runs.}
\item{\code{update_till_convergence}}{Logical; whether automatic convergence
updating was requested.}
\item{\code{min_R}}{The convergence threshold used for the potential scale
reduction factor.}
\item{\code{min_eff}}{The minimum effective sample size threshold used for
convergence assessment.}
\item{\code{silent}}{Logical; whether progress messages were suppressed.}
\item{\code{do_cpp}}{Logical; whether the C++ sampler was used.}
\item{\code{MH}}{Logical; whether Metropolis-Hastings updates were used.}
\item{\code{slicesampler_stepsize}}{Step-out size used by the slice sampler.}
\item{\code{log_prior_fun}}{Prior function used for the AFT transition-model
parameters.}
\item{\code{rescale_times}}{Logical; whether to rescale input times by the median of finite \code{R}.}
\item{\code{n_update}}{Integer; counter of the number of times the model was updated.}
\item{\code{seed_chains}}{Integer; seeds set at initialization of the Gibbs sampler.}
}
}
\description{
Estimates the Bayesian progressive three-state model for interval-censored
three-state screening outcomes with censoring after intervention
(Klausch et al., 2023). The model is fitted using a Gibbs sampler with data
augmentation for the latent event times \code{X} and \code{S}, and either slice-sampling
or Metropolis-Hastings updates for the transition-model parameters. MCMC chains
are processed sequentially.
}
\details{
See \code{\link[=bayestsm]{bayestsm()}}. Contrary to \code{\link[=bayestsm]{bayestsm()}}, the MCMC chains are run sequentially using a simple loop over chains. No
parallel backend is started or required. All else is the same.
}
\examples{
\dontrun{
library(BayesTSM)

# Generate data
dat = gendat(
  n = 1000,
  p = 2,
  r = 0,
  sigma.X = 0.3,
  mu.X = 2,
  beta.X = c(0.5, 0.5),
  sigma.S = 0.5,
  mu.S = 1,
  beta.S = c(0.5, 0.5),
  dist.X = "weibull",
  dist.S = "weibull",
  v.min = 1,
  v.max = 5,
  Tmax = 2e2,
  mean.rc = 10
)

# Run sequential Gibbs sampler with data augmentation and slice sampling
mod_slice = bayestsm_seq(
  d = dat$d,
  L = dat$L,
  R = dat$R,
  Z.X = dat[, c("Z.1", "Z.2")],
  Z.S = dat[, c("Z.1", "Z.2")],
  mc = 1e4,
  warmup = 5e2,
  thinning = 10,
  chains = 2,
  update_till_convergence = FALSE,
  MH = FALSE,
  dist.X = "weibull",
  dist.S = "weibull"
)

# Run sequential Gibbs sampler with Metropolis sampling of the parameters
mod_MH = bayestsm_seq(
  d = dat$d,
  L = dat$L,
  R = dat$R,
  Z.X = dat[, c("Z.1", "Z.2")],
  Z.S = dat[, c("Z.1", "Z.2")],
  mc = 1e4,
  warmup = 5e2,
  thinning = 10,
  chains = 2,
  update_till_convergence = FALSE,
  MH = TRUE,
  dist.X = "weibull",
  dist.S = "weibull"
)
}
}
\references{
Klausch, T., Akwiwu, E. M. U., van de Wiel, M. A., Coupé, V. M. H., and
Berkhof, J. (2023). A Bayesian accelerated failure time model for interval
censored three-state screening outcomes. \emph{The Annals of Applied
Statistics}, 17(2), 1285--1306. \doi{10.1214/22-AOAS1669}
}
\seealso{
\code{\link[=bayestsm]{bayestsm()}} \code{\link[=log_aft_prior]{log_aft_prior()}} \code{\link[=ppCIF]{ppCIF()}} \code{\link[=search_prop_sd]{search_prop_sd()}} \code{\link[=trim.mcmc]{trim.mcmc()}}
}
