% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gendat.r
\name{gendat}
\alias{gendat}
\title{Generate interval-censored three-state screening data}
\usage{
gendat(
  n,
  p = 3,
  p.discrete = 0,
  r = 0.1,
  s = 1,
  sigma.X = 1/2,
  mu.X = 1,
  beta.X = NULL,
  sigma.S = 1/2,
  mu.S = 1,
  beta.S = NULL,
  cor.X.S = 0,
  Tmax = 20,
  v.min = 1,
  v.max = 6,
  mean.rc = 40,
  dist.X = "weibull",
  dist.S = "weibull",
  do.b.XS = F,
  b.XS = NULL
)
}
\arguments{
\item{n}{Integer; number of individuals to simulate.}

\item{p}{Integer; number of multivariate normally distributed covariates.}

\item{p.discrete}{Integer; number of additional discrete covariates. Currently
only \code{0} or \code{1} is supported.}

\item{r}{Common correlation parameter used for the continuous covariates.}

\item{s}{Standard deviation used for each continuous covariate.}

\item{sigma.X}{Scale parameter for the first transition model (\code{X}).}

\item{mu.X}{Intercept for the first transition model (\code{X}).}

\item{beta.X}{Numeric vector of regression coefficients for the continuous and
discrete covariates in the first transition model. Should have length equal
to the total number of covariates.}

\item{sigma.S}{Scale parameter for the second transition model (\code{S}).}

\item{mu.S}{Intercept for the second transition model (\code{S}).}

\item{beta.S}{Numeric vector of regression coefficients for the continuous and
discrete covariates in the second transition model. Should have length equal
to the total number of covariates.}

\item{cor.X.S}{Correlation between the latent errors of \code{X} and \code{S} when
\code{dist.X = "bv-lognormal"}.}

\item{Tmax}{Integer; maximum number of screening visits generated per
individual.}

\item{v.min}{Minimum gap between two consecutive screening visits.}

\item{v.max}{Maximum gap between two consecutive screening visits.}

\item{mean.rc}{Mean of the exponential random censoring time distribution.}

\item{dist.X}{Character string specifying the distribution used to simulate
\code{X}. Supported values are \code{"weibull"}, \code{"loglog"}, \code{"lognormal"}, and
\code{"bv-lognormal"}. If \code{dist.X = "bv-lognormal"}, then \code{X} and \code{S} are
simulated jointly from a bivariate log-normal model with latent-error
correlation \code{cor.X.S}, and the value of \code{dist.S} is ignored.}

\item{dist.S}{Character string specifying the distribution used to simulate
\code{S}. Supported values are \code{"weibull"}, \code{"loglog"}, and \code{"lognormal"}. This
argument is ignored when \code{dist.X = "bv-lognormal"}.}

\item{do.b.XS}{Logical; whether to include a dependence term proportional to
\code{log(X)} in the linear predictor for \code{S}.}

\item{b.XS}{Numeric coefficient multiplying \code{log(X)} when \code{do.b.XS = TRUE}.}
}
\value{
A \code{data.frame} with one row per individual. It contains:
\describe{
\item{\code{L}}{Left endpoint of the observed event interval.}
\item{\code{R}}{Right endpoint of the observed event interval. Right-censored
observations have \code{R = Inf}.}
\item{\code{d}}{Observed event category.}
\item{\code{X}}{Simulated latent time from baseline to the first transition.}
\item{\code{S}}{Simulated latent time from the first to the second transition.}
\item{\code{Z}}{Simulated covariates, included when \code{p > 0}.}
}
}
\description{
Simulates interval-censored three-state screening data with covariates,
latent transition times, screening visit schedules, and observed event
categories as described by Klausch et al. (2023).
}
\details{
The function first simulates covariates from a multivariate normal
distribution with exchangeable correlation structure and optional additional
Bernoulli covariate. Given these covariates, latent transition times \code{X} and
\code{S} are generated from accelerated failure time models on the log-time scale.

The total time to the second event is \code{X + S}. Individuals are then assigned
a sequence of screening visit times. The first screening time is sampled
uniformly on the interval from \code{v.min} to \code{v.max}, and each subsequent
screening time is generated by adding a uniform increment between \code{v.min} and
\code{v.max} to the previous visit time. Follow-up is administratively censored at
a random censoring time generated from an exponential distribution with mean
\code{mean.rc}.

The observed data are constructed from the screening interval in which the
first detected transition occurs. The event indicator \code{d} equals \code{2} if the
first transition time \code{X} falls in the observed interval but the second event
time \code{X + S} does not; \code{d} equals \code{3} if both transitions have occurred by the
same observed screening interval; and \code{d} equals \code{1} if no event is observed
during follow-up, in which case \code{R = Inf}.

When \code{do.b.XS = TRUE}, the second transition time depends additionally on
\code{log(X)} through coefficient \code{b.XS}. This induces dependence between the two
transition times beyond the shared covariate effects. Alternatively, when
\code{dist.X = "bv-lognormal"}, both \code{X} and \code{S} are generated jointly from a
bivariate log-normal construction with correlation \code{cor.X.S}; in that case
the separate specification through \code{dist.S} is overridden.

This function is primarily intended for simulation studies, examples, and
testing of the model-fitting functions in the package.
}
\examples{
dat = gendat(
              n = 1000,  # Sample size
              p = 2,     # Number of normal distributed covariates
              r = 0,     # Correlation of the covariates
              sigma.X = 0.3,         # True scale parameter
              mu.X    = 2,           # True intercept parameter
              beta.X  = c(0.5,0.5),  # True slope parameters
              sigma.S = 0.5,         # True scale parameter
              mu.S    = 1,           # True intercept parameters
              beta.S  = c(0.5,0.5),  # True slope parameters
              dist.X  = 'weibull',   # Distribution of X
              dist.S  = 'weibull',   # Distribution of S
              v.min   = 1,           # Min time between screening moments
              v.max   = 5,           # Max time between screening moments
              Tmax    = 2e2,         # Max number of screening times
              mean.rc = 10           # Mean time to right censoring
            )

}
