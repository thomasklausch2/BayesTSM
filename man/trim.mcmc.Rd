% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trim.mcmc.r
\name{trim.mcmc}
\alias{trim.mcmc}
\title{Trim and thin an \code{mcmc.list}}
\usage{
trim.mcmc(obj, burnin = 1, end = nrow(as.matrix(obj[1])), thinning = 1)
}
\arguments{
\item{obj}{An object of class \code{mcmc.list}.}

\item{burnin}{Integer; first iteration to retain. Defaults to \code{1}.}

\item{end}{Integer; last iteration to retain. Defaults to the number of rows
in the first chain.}

\item{thinning}{Integer; thinning interval. Defaults to \code{1}.}
}
\value{
An object of class \code{mcmc.list} containing the trimmed and thinned
chains.
}
\description{
Convenience function for trimming burn-in iterations and applying thinning to
an object of class \code{mcmc.list}.
}
\details{
The function subsets each chain in \code{obj} to the iterations
\code{seq(burnin, end, thinning)} and reconstructs the result as an \code{mcmc.list}.

Note that the argument name is \code{thinning} to match the existing function
definition.
}
